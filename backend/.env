# Backend environment variables
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=uploads
USE_STUB=0
# Optional if using Whisper API (not needed for local whisper)
HUGGINGFACE_DEVICE=cpu
# Directory for uploaded files
UPLOAD_DIR=backend/uploads